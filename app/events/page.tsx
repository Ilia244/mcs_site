"use client";import {useEffect,useState} from "react";import Link from "next/link";import {supabase} from "@/lib/supabase"
export default function EventsPage(){const[p,setP]=useState<any[]>([]);useEffect(()=>{supabase.from("posts").select("id,title,content,published_at,post_tags(tags(name,color,icon))").eq("status","published").eq("type","event").order("published_at",{ascending:false}).then(({data})=>setP(data||[]))},[]);return <div className="portal-bg min-h-screen text-white"><main className="max-w-5xl mx-auto px-5 py-14"><Link href="/minecraft" className="text-cyan-300 text-sm">← MCSへ戻る</Link><h1 className="text-4xl font-bold mt-7 mb-10">🎉 イベント</h1><div className="space-y-4">{p.map(x=><Link href={`/news/${x.id}`} key={x.id} className="portal-panel block-hover"><h2 className="text-2xl font-bold">{x.title}</h2><p className="text-gray-400 mt-2 whitespace-pre-wrap">{x.content}</p></Link>)}{!p.length&&<div className="portal-panel text-gray-400">現在予定されているイベントはありません。</div>}</div></main></div>}
